1. Написати програму, яка в якості вхідних параметрів приймає кількість годин (Hours) та платіж за годину (Rate), визначаючи сумарну вартість (Pay). Реалізовувати у вигляді користувацької функції, діалог с користувачем має вигляд:
2. Написати функцію, яка приймає в якості параметру score (оцінку) та повертає grade (бал) як строку.
3. Виконати завдання початкового рівня(1) 5-ї лабораторної роботи курсу програмування. Перевірка існування файлу має здійснюватися із використанням блоку try/catch.
